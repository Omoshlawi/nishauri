import 'package:flutter/material.dart';

class DrugDeliveryWizardForm extends StatelessWidget {
  const DrugDeliveryWizardForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
